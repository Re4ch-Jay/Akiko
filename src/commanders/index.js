const makeCommand = require('./makeCommand');
const queryCommand = require('./queryCommand');
const readFileCommand = require('./readFileCommand');

module.exports = {
    makeCommand,
    queryCommand,
    readFileCommand,
}
